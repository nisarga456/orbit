package com.orbit.test;

import org.openqa.selenium.Keys;
import org.testng.annotations.Test;

import com.orbit.generics.base;
import com.orbit.pom.information;
import com.orbit.pom.notification;

public class testinformation extends base {
	
	/*
	 * @Test(priority=2)
	 * 
	 * public void information() throws InterruptedException{
	 * 
	 * information info=new information(driver); info.setinformation();
	 * action.sendKeys(Keys.ESCAPE).build().perform(); sa.assertAll(); }
	 */

	/*
	 * @Test(priority=1)
	 * 
	 * public void notifications() throws InterruptedException{
	 * 
	 * notification nn=new notification(driver); nn.setnotification();
	 * action.sendKeys(Keys.ESCAPE).build().perform(); sa.assertAll(); }
	 * 
	 */
	
	
}
